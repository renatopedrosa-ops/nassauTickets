# Servidor
PORT=3001
APP_TIMEZONE=America/Recife
CORS_ORIGIN=http://localhost:5173

# Banco de dados MySQL 8.0
DB_HOST=localhost
DB_PORT=3306
DB_USER=nassau
DB_PASSWORD=nassau123
DB_NAME=nassau_tickets

# Autenticação (troque em produção!)
JWT_SECRET=troque-este-segredo
JWT_EXPIRES_IN=10h

# Expediente (RN01). Use false para testar fora do horário.
OPEN_HOUR=7
CLOSE_HOUR=17
ENFORCE_BUSINESS_HOURS=true
